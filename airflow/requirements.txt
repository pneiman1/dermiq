# Cosmos renders the dbt project as an Airflow task group.
astronomer-cosmos==1.8.*
# Runtime deps for the bind-mounted platform_core + dermiq.ingestion code
# (the extract task): Snowflake load (write_pandas needs pandas/pyarrow),
# Postgres read, structured logging, settings.
snowflake-connector-python[pandas]==3.*
psycopg2-binary==2.9.*
structlog>=24
pydantic-settings>=2
# scikit-learn for the weekly patient-clustering DAG (dermiq.ml.clustering).
scikit-learn>=1.4
# sentence-transformers for the daily RAG corpus refresh (dermiq.rag). Pulls torch,
# so it materially increases the image size — the corpus build embeds locally.
sentence-transformers>=2.7
